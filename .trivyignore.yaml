vulnerabilities:
  # golang.org/x/text, which every Go binary below vendors
  - id: CVE-2026-56852
    paths:
      - "usr/local/bin/gopls"
      - "usr/local/bin/terraform"
    expired_at: 2026-10-31
    statement: |
      gopls 0.23.0, the latest release, depends on golang.org/x/text v0.38.0,
      and Terraform 1.15.8 on v0.37.0. The fix is in v0.39.0, so this clears
      once each upstream releases a build that picks it up.

  # terraform
  - id: CVE-2026-27145
    paths:
      - "usr/local/bin/terraform"
    expired_at: 2026-10-31
    statement: |
      Terraform 1.15.7 was built with Go 1.25.10, which is affected by this CVE.
      The upstream development branch already uses Go 1.26.4, so the fix is
      expected in the next Terraform release.
  - id: CVE-2026-42504
    paths:
      - "usr/local/bin/terraform"
    expired_at: 2026-10-31
    statement: |
      Terraform 1.15.7 was built with Go 1.25.10, which is affected by this CVE.
      The upstream development branch already uses Go 1.26.4, so the fix is
      expected in the next Terraform release.
  - id: CVE-2026-39822
    paths:
      - "usr/local/bin/terraform"
      - "usr/local/bin/terraform-ls"
    expired_at: 2026-10-31
    statement: |
      Terraform 1.15.7 was built with Go 1.25.10, which is affected by this CVE.
      The upstream development branch was upgraded to Go 1.26.4, but it is till affected.
      Terraform Language Server 0.38.8 was built with Go 1.25.11, which is affected by this CVE.
      The upstream development branch has not yet been updated to a Go version that is not affected by this CVE.
  - id: GHSA-hrxh-6v49-42gf
    paths:
      - "usr/local/bin/terraform"
    expired_at: 2026-10-31
    statement: |
      Terraform repository currently uses google.golang.org/grp v1.80.0, so no fix is available yet.
